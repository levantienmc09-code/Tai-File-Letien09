services:
  - type: web
    name: fileshare
    env: php
    plan: free
    buildCommand: "chmod -R 755 . && mkdir -p data && chmod 755 data"
    startCommand: "php -S 0.0.0.0:8000 index.php"
    envVars:
      - key: PHP_VERSION
        value: 8.2
