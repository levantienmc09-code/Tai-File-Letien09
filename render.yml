services:
  - type: web
    name: fileshare
    env: docker
    plan: free
    docker:
      dockerfilePath: Dockerfile
    envVars:
      - key: PHP_ENV
        value: production
    healthCheckPath: /
    autoDeploy: true
