services:
  - type: web
    name: fileshare
    env: php
    plan: free
    buildCommand: |
      mkdir -p data
      chmod 755 data
    startCommand: "php -S 0.0.0.0:10000 index.php"
    disk:
      name: data
      mountPath: /var/www/html/data
      sizeGB: 1
